"use client";

import { useState } from "react";
import { ButtonPrimary } from "@/components/shared/button-primary";
import { ButtonSecondary } from "@/components/shared/button-secondary";
import { toast } from "sonner";
import {
  Dialog,
  DialogContent,
  DialogHeader,
  DialogTitle,
} from "@/components/ui/dialog";
import { PagamentoSelector } from "@/components/totem/pagamento-selector";

interface PagamentoModalProps {
  open: boolean;
  onOpenChange: (open: boolean) => void;
  comandaId: string;
  clienteNome: string;
  total: number;
  formaPagamento: string | null;
  quantidadeParcelas: number;
  onSuccess: () => void;
  formaPagamentoId?: string | null;
  onPaymentMethodChange?: (id: string, nome:            string, max: number) => void;
}

export function PagamentoModal({
  open,
  onOpenChange,
  comandaId,
  clienteNome,
  total,
  formaPagamento,
  quantidadeParcelas: initialQuantidadeParcelas,
  onSuccess,
  formaPagamentoId: initialFormaPagamentoId,
  onPaymentMethodChange,
}: PagamentoModalProps) {
  const [loading, setLoading] = useState(false);
  const [selectedFormaPagamentoId, setSelectedFormaPagamentoId] = useState<
    string | null
  >(initialFormaPagamentoId || null);
  const [selectedFormaPagamentoNome, setSelectedFormaPagamentoNome] = useState<
    string | null
  >(formaPagamento || null);
  const [selectedQuantidadeParcelas, setSelectedQuantidadeParcelas] = useState<
    number
  >(initialQuantidadeParcelas);

  async function handlePagar() {
    setLoading(true);
    try {
      const res = await fetch(`/api/comandas/${comandaId}/pagar`, {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          formaPagamentoId: selectedFormaPagamentoId || null,
          quantidadeParcelas: selectedQuantidadeParcelas ?? 1,
        }),
      });

      if (!res.ok) throw new Error();
      toast.success("Comanda paga com sucesso!");
      onOpenChange(false);
      onSuccess();
    } catch {
      toast.error("Erro ao processar pagamento");
    } finally {
      setLoading(false);
    }
  }

  const handlePaymentMethodChange = (
    id: string,
    nome: string,
    max: number
  ) => {
    setSelectedFormaPagamentoId(id);
    setSelectedFormaPagamentoNome(nome);
    if (max === 1) {
      setSelectedQuantidadeParcel            Parcelas(1);
    }
    onPaymentMethodChange?.(id, nome, max);
  };

  const handleParcelasChange = (qtd: number) => {
    setSelectedQuantidadeParcelas(qtd);
  };

  return (
    <Dialog open={open} onOpenChange={onOpenChange}>
      <DialogContent className="sm:max-w-md">
        <DialogHeader>
          <DialogTitle className="text-title-md text-ink">
            Confirmar Pagamento
          </DialogTitle>
        </DialogHeader>

        <div className="space-y-4">
          {!formaPagamento ? (
            <PagamentoSelector
              formaPagamentoId={selectedFormaPagamentoId || ""}
              quantidadeParcelas={selectedQuantidadeParcelas}
              total={total}
              onSelectForma={handlePaymentMethodChange}
              onSelectParcelas={handleParcelasChange}
            />
          ) : (
            <div className="bg-surface-soft rounded-lg p-4 space-y-2">
              <div className="flex justify-between">
                <span className="text-body-md text-body">Cliente</span>
                <span className="text-body-md text-ink font-medium">
                  {clienteNome}
                </span>
              </div>
              <div className="flex justify-between">
                <span className="text-body-md text-body">Forma de Pagamento</span>
                <span className="text-body-md text-ink font-medium">
                  {selectedFormaPagamentoNome || "—"}
                </span>
              </div>
              {selectedQuantidadeParcelas > 1 && (
                <div className="flex justify-between">
                  <span className="text-body-md text-body">Parcelas</span>
                  <span className="text-body-md text-ink font-medium">
                    {selectedQuantidadeParcelas}x de R${(
                      total / selectedQuantidadeParcelas
                    ).toFixed(2)}
                  </span>
                </div>
              )}
              <div className="flex justify-between pt-2 border-t border-hairline">
                <span className="text-title-sm text-ink">Total</span>
                <span className="text-title-sm text-ink font-medium">
                  R$ {total.toFixed(2)}
                </span>
              </div>
            </div>
          )}

          <div className="flex gap-3">
            <ButtonSecondary
              type="button"
              onClick={() => onOpenChange(false)}
              className="flex-1"
            >
              Cancelar
            </ButtonSecondary>
            <ButtonPrimary onClick={handlePagar} disabled={loading} className="flex-1">
              {loading ? "Processando..." : "Confirmar Pagamento"}
            </ButtonPrimary>
          </div>
        </div>
      </DialogContent>
    </Dialog>
  );
}
